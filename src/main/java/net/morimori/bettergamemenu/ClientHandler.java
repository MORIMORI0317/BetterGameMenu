package net.morimori.bettergamemenu;

import net.minecraftforge.client.event.GuiOpenEvent;
import net.minecraftforge.eventbus.api.SubscribeEvent;

public class ClientHandler {
   /* @SubscribeEvent
    public static void onOpenGUI(GuiOpenEvent e) {
        System.out.println(e.getGui().getClass());
        //if (e.getGui() instanceof MenuScreens && ((IngameMenuScreen) e.getGui()).showPauseMenu && !(e.getGui() instanceof BetterGameMenuScreen)) {
        //     e.setGui(new BetterGameMenuScreen());
        //  }
    }*/
}